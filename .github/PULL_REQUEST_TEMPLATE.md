<!--
  작성 기준은 CONVENTIONS.md "형식 — 훑어읽을 수 있어야 한다" 참조.
  판정: 30초 훑어서 "무엇이 왜 문제였고 어떻게 고쳤나"가 잡히는가.
  분량은 영향 범위에 비례한다 — 작은 수정에 긴 본문은 오히려 판단력 부족으로 읽힌다.
  해당 없는 절은 지운다. 빈 절을 남기면 템플릿이 잡음이 된다.
-->

## Description

<!--
  이 PR이 해결하는 문제를 먼저 쓴다 — "무엇을 바꿨나"가 아니라 "왜 이게 문제였나".
  리뷰어가 diff를 읽기 전에 판단 근거를 얻어야 한다.
  결함 수정이면 재현 조건과 영향 범위를 여기에.
-->

## Changes Made

<!-- 불릿으로. 한 불릿 = 한 변경. 변경이 둘 이상이면 소제목으로 나눈다. -->

-

## Testing

<!-- 숫자를 적는다. 컨테이너가 필요한 항목은 표시한다. -->

- 단위 (`pytest tests/unit`) — N passed
- 통합 (`pytest tests/integration`) — N passed *(컨테이너 필요)*

## Related

<!--
  근거 문서·관련 PR. 이 레포는 이슈 트래커 대신 ADR과 backlog를 쓴다.
  예: ADR 0005, docs/backlog.md #37, #18 머지 후 배포 필요
  없으면 절을 지운다.
-->

## Checklist

- [ ] `ruff check` · `ruff format --check` · `vulture` 통과
- [ ] 테스트가 전부 통과한다
- [ ] **결함 수정인 경우**: 새 테스트가 **수정 전 코드에서 실패**하는 것을 확인했다
- [ ] 관련 ADR·문서를 현행에 맞게 갱신했다
- [ ] 선행 조건(다른 PR 머지·배포·마이그레이션)이 있으면 Description 최상단에 적었다
