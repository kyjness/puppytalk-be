# 00 · 운영 전제와 범위 (Operating Envelope & Scope)

> **Phase 0 · Inception 산출물 — 확정 (Accepted, 2026-07-03).**
> 이 문서는 이후 모든 설계·ADR·복잡도 판정의 **단일 근거**다.
> 복잡도는 이 봉투 *안에서만* 정당화된다. 봉투 밖은 과잉으로 본다.

---

## 1. 목적

AI 기능이 아니라 **"현실적 트래픽을 가정한 운영 등급 백엔드 설계"**를 이 포트폴리오의 차별점으로 삼는다.
차별점의 증명은 *기능의 개수*가 아니라 **"각 복잡도가 명시된 운영 전제로 정당화된다"**는 판단력이다.
따라서 먼저 **전제(2)**를 못 박고, 그 위에서 **운영 과제(3)**를 분류하고, **재건 순서(4)**를 정한다.

---

## 2. 운영 전제 (Operating Envelope)

| 축 | 전제(확정) | 함의 |
|----|-----------|------|
| 규모 | 평상시 수천~수만 DAU | 단일 인스턴스로도 평시 처리 가능, 피크가 문제 |
| 핫스팟 | 인기글 1건에 **초당 수백~수천 조회** | 조회 경로가 최적화 1순위 (버퍼링·캐시) |
| 쓰기 | 신규 write는 초당 수십 규모 | 강한 정합성 필요한 write는 정직하게 트랜잭션으로 |
| 서버 | 멀티 인스턴스 **3~10대** (수평 확장) | 상태는 인스턴스 밖(Redis/DB)에. 로컬 상태 금지 |
| 배포 | 무중단 롤링 / 블루-그린 | 마이그레이션 하위호환, graceful shutdown 필요 |
| 가용성 | **99.9%** (월 ~43분 다운 허용) | 외부 I/O는 fallback 우선(가용성 > 순간 정합성) |

**상한 — 이걸 넘는 복잡도는 과잉으로 판정한다:**
멀티리전 · 금융권 강정합성 · 99.99%+ · 초당 수만 건 *신규 write* · exactly-once 배송.

---

## 3. 운영 과제 목록

각 과제는 **채택 시 ADR**로 "왜·트레이드오프·안 한 선택"을 남긴다.
[`backlog.md`](backlog.md)(버그·최적화 백로그) 항목번호를 근거로 연결한다.

### 3-A. 횡단 관심사 (Cross-cutting — 여러 도메인 공통, 기반층에서 먼저 확정)

| 과제 | 무엇을 결정하나 | 백로그 근거 |
|------|----------------|------------|
| **설정·환경** | pydantic-settings 전환, 환경별 필수값 검증(프로덕션 가드) | #14 |
| **식별자 전략** | 내부 PK / 공개 ID 형식과 역할 분리 | — |
| **API 일관성** | 응답 계약(`ApiResponse[T]`), `request_id` 전파 | — |
| **트랜잭션 규약** | Unit of Work 경계(CUD = 단일 `begin()`), 커밋/롤백 원칙 | #3 |
| **캐시 전략** | Redis 캐시 계층(인증 상태·조회 등), 무효화·TTL 원칙 | #7 |
| **Rate Limiting** | 분산 환경 레이트리밋(알고리즘·폴백) | — |
| **복원력** | 외부 I/O(S3·Redis) fallback/degradation 정책 | #1 |
| **페이지네이션** | 목록 규약 = Cursor 기반, `total` 처리 방침 | #5, #6, #10 |
| **낙관적 락** | *필요한 곳만* version 적용 (동시수정 충돌 실재하는 경로) | — |
| **관측성** | 구조화 로그 · 메트릭 · 추적(request_id) | #20 |
| **인덱스 규약** | 조회 패턴 기반 인덱스 설계, 중복 인덱스 제거 | #13, #16 |

### 3-B. 도메인별 과제

| 도메인 | 운영 과제 | 백로그 근거 |
|--------|----------|------------|
| **posts** (핵심·최고부하) | 조회수 버퍼링(Redis 버퍼+lifespan 주기 flush+분산락), 검색(ILIKE 이스케이프/pg_trgm), 해시태그 동기화, 목록 N+1(대표견만), COUNT 비용 | #2, #4, #10, #11, #12, #17 |
| **auth / users** | 인증 캐싱, 정지 시 토큰 즉시 무효화, bcrypt 폴백, 차단(UserBlock) 인덱스 | #7, #8, #9, #13, #3 |
| **media** | 이미지 업로드(멱등성), 스토리지 삭제 정합성(고아 파일 방지), 잡 락(Lua CAS) | #1 |
| **comments** | 트리 페이지네이션(하드리밋 제거), 좋아요 카운트 중복 제거 | #6, #15 |
| **chat** | WebSocket Stateless, 워커 간 Redis Pub/Sub, 미읽음 카운트 쿼리 최적화 | #16, #19 |
| **notifications** | 비동기 알림(워커 큐), 전달(SSE/pub-sub) | — |
| **reports / admin** | 신고 목록 페이지네이션(메모리 슬라이스 제거) | #5 |
| **dogs / likes** | 도메인 정리(대표견, 좋아요 카운트 일관성) | #11, #15 |

### 3-C. 운영(Ops) — 별도 묶음, 마지막

배포(Docker·ECS·CI/CD 재정의) · 모니터링 · 로그. **Phase Ops에서 설계.**

---

## 4. 도메인 재건 순서 (의존도 기준)

앞 단계가 뒤 단계의 전제가 되도록 **의존도 낮은 기반부터** 재건한다(하이브리드: 검증된 로직은 이식).

1. **기반층(횡단)** — `db/`(세션·엔진), `core/config`(설정), `common/responses`(ApiResponse), `core/ids`(식별자), 로깅. → 3-A의 토대.
2. **auth / users** — 인증은 거의 모든 도메인의 전제.
3. **media** — 업로드는 posts·dogs·users가 의존.
4. **posts** — 핵심·최고부하(조회수·검색·페이지네이션). 가장 공들일 도메인.
5. **comments · likes** — posts에 의존.
6. **dogs** — users에 의존.
7. **chat · notifications** — 실시간/비동기.
8. **reports · admin** — 다른 도메인 데이터를 소비하는 관리 기능.
9. **Ops** — 배포·모니터링·로그 (Phase Ops).

각 도메인 재건 = Construction 단위. 단위마다 검증→계획(승인)→수정→검사→커밋.

---

## 5. 확정된 결정 (Phase 0 종료)

1. **운영 전제(2절)** — 초안 그대로 확정.
2. **AI 기능** — **이번 범위에서 완전히 배제.** 스키마·구조에 임베딩 훅 등 여지를 두지 않는다.
   차별점은 오직 *운영 등급 백엔드 설계*.
3. **재건 순서(4절)** — 기반→auth→media→posts→… 확정.

> Inception 종료. 다음 단계(**Elaboration**): `01-architecture.md` — 3-A 횡단 관심사의 실제 결정
> (캐시·식별자·API 일관성·트랜잭션·Rate Limit·복원력·페이지네이션·관측성·인덱스)을 확정하고
> load-bearing 결정은 `docs/adr/`로 근거화.
